<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Lawyer"> <!--테이블 하나마다 mapper하나!  mapper namespace는 mapper에 대한 식별자이다 
								자바 쪽에서 내가 원하는 쿼리에 접근하기위해서!-->
	<!-- 회원가입 (id는 쿼리를 만들 메소드의 이름과 동일하게 만들어준다. -->
	<insert id ="join" parameterType="UserVO"> <!--id는 메소드이름 parameterTYPE은 변수의 타입! 
	회원가입은 여러개가 넘어오니까 클래스로 넘어오게해주었다. 원래는 parameterType에 com.member.domain.vo.MemberVO
	다 써주어야하는데 config.xml에서
	<typeAliases>
		<typeAlias type="com.member.domain.vo.MemberVO" alias ="memberVO"/>
	</typeAliases> 이처리를 해주어서 MemberVO만 써도 된다
										 -->
	insert into tbl_user_register
	(user_num, user_id, user_name, user_email, user_pw, user_phone_num,user_gender,user_birth,user_kakao_email)
	values(#{userNum}, #{userId},#{userName}, #{userEmail}, #{userPw}, #{userPw},#{userPhoneNum},#{userGender},#{userBirth},#{userKakaoEmail})
	<!--#{} 게터메소드가 호출되서 들어가는것이다 마이바티스의 문법! -->
	</insert>
	<insert id ="join" parameterType="LawyerVO"> <!--id는 메소드이름 parameterTYPE은 변수의 타입! 
	회원가입은 여러개가 넘어오니까 클래스로 넘어오게해주었다. 원래는 parameterType에 com.member.domain.vo.MemberVO
	다 써주어야하는데 config.xml에서
	<typeAliases>
		<typeAlias type="com.member.domain.vo.MemberVO" alias ="memberVO"/>
	</typeAliases> 이처리를 해주어서 MemberVO만 써도 된다
										 -->
	insert into tbl_lawyer_register
	(lawyer_num, lawyer_id, lawyer_name, lawyer_email, lawyer_pw, lawyer_phone_num,lawyer_gender,lawyer_birth,lawyer_kakao_email)
	values(#{lawyerNum}, #{lawyerId},#{lawyerName}, #{lawyerEmail}, #{lawyerPw}, #{lawyerPw},#{userPhoneNum},#{userGender},#{userBirth},#{userKakaoEmail})
	<!--#{} 게터메소드가 호출되서 들어가는것이다 마이바티스의 문법! -->
	</insert>
	
	
</mapper>