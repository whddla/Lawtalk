<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserWrite"> 
   <insert id ="userWrtie" parameterType="userwriteVO"> 
   	insert into tbl_user_write
	(title, content, write_date)
	values(#{title}, #{content}, #{writeDate});
   </insert>
   
   <select id ="checkUserWrite" parameterType="_int">
   	select userWriteNum, requestNum, userNum, title, content, writeDate
	from tbl_user_write
	where userWriteNum = #{userWriteNum}
   </select>
</mapper>